california_civilwar_cat = {
    reintergrate_californian_state_dec = {
        allowed = {
            OR = {
				tag = CAL
				tag = CAZ
				tag = CAS
			}
        }
        icon = QAT_example
        fire_only_once = yes
		on_map_mode = map_and_decisions_view
        days_remove = 50
        cost = 50
        modifier = {
            command_power_gain = -0.1
        }
        target_trigger = {
            FROM = {
                NOT = {
                   is_core_of = prev
                } #var:original_tag
            }
            controls_state = FROM
        }
        available = {
            FROM = {
                resistance < 50
                compliance > 25
            }
        }
        state_target = yes
        target_array = global.california_states
        remove_effect = {
            add_state_core = FROM
        }
        cancel_trigger = {
            controls_state = FROM
        }
    }
}